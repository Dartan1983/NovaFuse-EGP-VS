name: NovaFuse-EGP-VS Validation

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  validation:
    runs-on: windows-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup PowerShell
      uses: azure/powershell@v1
      with:
        azPSVersion: "latest"
        inlineScript: |
          Write-Host "PowerShell Version: $($PSVersionTable.PSVersion)"
          
    - name: Run Verification Harness
      shell: powershell
      run: |
        cd verifier
        .\verify.ps1
        
    - name: Validate Certificate
      shell: powershell
      run: |
        if (Test-Path "verifier\artifacts\certs\certificate-*.json") {
          $cert = Get-Content "verifier\artifacts\certs\certificate-*.json" | ConvertFrom-Json
          Write-Host "Certificate Status: $($cert.results.overall)"
          Write-Host "Certificate ID: $($cert.metadata.certificate_id)"
          
          if ($cert.results.overall -eq "PASS") {
            Write-Host "✅ Certificate validation passed"
          } else {
            Write-Host "❌ Certificate validation failed"
            exit 1
          }
        } else {
          Write-Host "❌ No certificate found"
          exit 1
        }
        
    - name: Test Bundle Generation
      shell: powershell
      run: |
        .\Generate-USB-Bundle-Clean.ps1 -OutputPath .\test-bundle -SkipSigning
        
        if (Test-Path ".\test-bundle\NovaFuse-EGP-VS-v0.1.0") {
          Write-Host "✅ Bundle generation successful"
        } else {
          Write-Host "❌ Bundle generation failed"
          exit 1
        }
        
    - name: Validate Bundle Integrity
      shell: powershell
      run: |
        $manifestPath = ".\test-bundle\NovaFuse-EGP-VS-v0.1.0\RELEASE-HASHES.txt"
        if (Test-Path $manifestPath) {
          Write-Host "✅ Bundle manifest found"
          
          # Check if we can read and parse the manifest
          $lines = Get-Content $manifestPath
          $hashLines = $lines | Where-Object { $_ -match "^[a-f0-9]" }
          Write-Host "Bundle contains $($hashLines.Count) files with hashes"
        } else {
          Write-Host "❌ Bundle manifest not found"
          exit 1
        }
        
    - name: Upload Artifacts
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: verification-artifacts
        path: |
          verifier/artifacts/
          test-bundle/
        retention-days: 30
